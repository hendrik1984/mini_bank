<nav class="mb-4 navbar navbar-expand-lg">
    <div class="container">
        <%= link_to dashboard_path, :class => "navbar-brand" do %>
            <%= image_tag "/bank.svg", alt: "Bank Logo", class: "img-fluid", width: "30", height: "24" %>
        <% end %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample07" aria-controls="navbarsExample07" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <% if logged_in? %>
                    <% if current_user.role == "admin" %>
                        <li class="nav-item"><%= link_to "Admin Dashboard", admin_dashboard_path, class: "nav-link" %></li>
                    <% else %>
                        <li class="nav-item"><%= link_to "Dashboard", dashboard_path, class: "nav-link" %></li>
                    <% end %>
                    <li class="nav-item"><%= link_to "Transactions", transactions_path, class: "nav-link" %></li>
                    <li class="nav-item"><%= link_to "New Transactions", new_transaction_path, class: "nav-link" %></li>
                    <li class="nav-item">
                        <%= link_to "Logout", logout_path, method: :delete, data: { turbo_confirm: "Are you sure?", turbo_method: :delete }, class: "nav-link" %>
                        <%#= button_to "Logout", logout_path, method: :delete, form: {data: {turbo_confirm: 'Are you sure?'}} %>
                    </li>
                <% else %>
                    <li class="nav-item"><%#= link_to "Login", login_path, class: "nav-link" %></li>
                    <li class="nav-item"><%= link_to "Register", register_path, class: "nav-link" %></li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>
